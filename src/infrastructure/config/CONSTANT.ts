export const DATE_FORMAT = {
  FORMAT1: "YYYY/MM/DD",
  FORMAT2: "YYYY-MM-DD",
};

export const MESSAGE = {
  MES1: "Hello world",
};

export const REGEX = {};

export const ENV = {
  END_POINT: process.env.REACT_APP_API_END_POINT,
  TIMEOUT: parseInt(process.env.REACT_APP_API_TIMEOUT || "10000"),
};
